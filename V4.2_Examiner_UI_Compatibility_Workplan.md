# V4.2 Examiner UI Compatibility 工作计划

更新时间：2026-05-09

目标：在不触碰已验证正确的算法流程和绘图显示核心逻辑的前提下，逐步完成与 3D-Radar Examiner 的界面、交互和功能接口对齐。默认界面使用中文，英文界面通过语言设置切换。

## 进度标记

- `[ ]` 未开始
- `[~]` 开发中
- `[x]` 已完成
- `[hold]` 暂缓，等待资料、验证或算法设计

## 当前已完成基线

| 状态 | 模块 | 完成内容 | 备注 |
| --- | --- | --- | --- |
| [x] | 版本基线 | V4.1 已作为已验证可用版本基线 | 后续 V4.2 在此基础上开发 |
| [x] | 主界面 | 初步对齐 3D-Radar Examiner Ribbon、总览、探索布局 | 菜单高度已多轮压缩 |
| [x] | 中文化方向 | 默认中文菜单和按钮 | 仍有残留英文需要继续清理 |
| [x] | 探索页工具条 | 已按 3D Radar 顺序调整工具按钮 | 图标样式仍需继续贴近原软件 |
| [x] | 探索页测量 | 单击添加折点、鼠标移动预览线、双击完成、Esc 清空 | 需继续做状态细节收尾 |
| [x] | 总览页测量 | 已接入总览地图测量覆盖层 | 需手动验证交互细节 |
| [x] | 测量结果框 | 进入测量显示蓝色结果框，退出测量关闭结果框 | 后续接语言切换 |

## 第一优先级：低风险、高价值、最影响对标观感

| 状态 | 优先级 | 工作项 | 目标行为 | 实现策略 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| [x] | P0 | 全局中文化清理 | 默认界面所有可中文化文本显示中文 | 统一走语言字典，清理硬编码英文 | 已覆盖联动切片、测量框、Scan 右键菜单、主要视图标题 |
| [~] | P0 | 英文语言切换 | 用户在设置里切换英文后显示英文 | 建立 zh_CN/en_US 字典和刷新机制 | 已覆盖 Ribbon、文件菜单、联动切片、测量框、Scan 右键菜单；深层设置弹窗后续继续补齐 |
| [x] | P0 | Scan 图右键菜单 | 对齐 3D Radar 右键菜单 | 添加 Set Crosshair、Center View、Fit、Copy、Save 等动作 | 已接入 B/C/Width/Trace 图右键菜单 |
| [x] | P0 | 图像复制到剪贴板 | 右键可复制当前处理图像 | 使用当前视图渲染结果生成 QPixmap | 已实现，需人工粘贴验证 |
| [x] | P0 | 图像另存为 | 右键可保存 PNG/JPG/TIFF | QFileDialog + 当前视图截图/渲染 | 已实现 PNG/JPG/TIFF 保存 |
| [x] | P0 | 测量功能收尾 | 总览/探索交互一致，退出状态干净 | 统一按钮状态、结果框生命周期、Esc 行为 | 已完成退出测量关闭结果框，测量框随语言刷新 |
| [x] | P1 | 文件菜单精简 | 去掉与 Ribbon 重复的工程操作 | 保留文件级操作、语言、设置、导出 | 已保留新建、打开、保存、保存处理结果、加载模板、语言 |

## 第二优先级：中等难度、强 3D Radar 对标感

| 状态 | 优先级 | 工作项 | 目标行为 | 实现策略 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| [ ] | P1 | 探索 Pan | 拖拽平移切片视图 | 接入现有视图平移能力 | 选中按钮后拖拽验证 |
| [ ] | P1 | 探索 Zoom | 支持缩放或框选缩放 | 先接滚轮/视图缩放，框选后续 | 滚轮和按钮状态验证 |
| [x] | P1 | 探索 Measure | 测量长度和面积 | 已实现基础逻辑 | 继续手动回归 |
| [~] | P1 | Trace Interface 面板 | 点击弹出 3D Radar 风格参数窗 | 先保留参数和接口，不接复杂算法 | 点击按钮检查弹窗 |
| [ ] | P1 | Show Crosshair | 显示/隐藏十字线 | 接现有十字线显示状态 | 开关后 B/C/Trace 联动检查 |
| [ ] | P1 | Show Hyperbola | 显示/隐藏双曲线预览 | 先做占位叠加层和接口 | 点击后显示占位提示或开关状态 |
| [ ] | P1 | Show Timeground Marker | 显示地面时间标记 | 先做占位叠加层和接口 | 点击后显示占位提示或开关状态 |
| [ ] | P1 | Show Headers | 显示扫描头/文件头信息 | 做只读信息面板 | 打开当前数据头信息 |
| [ ] | P2 | Dual Depth Axis | 显示时间/深度双轴 | 先预留 UI；真实换算依赖介电参数 | 开关后轴区域显示占位 |
| [ ] | P1 | 总览 Select/Pan/Zoom | 总览地图基础交互对齐 | 接入当前地图控件能力 | 逐按钮验证 |
| [ ] | P2 | 总览 Move | 区域/轨迹移动接口 | 先做模式和确认框，不直接改数据 | 点击后提示风险并预留 |
| [ ] | P2 | Modify Track | 轨迹修正接口 | 先做按钮、状态、撤销接口占位 | 点击后状态栏/弹窗提示 |
| [ ] | P1 | Annotate | 点/线/面标注入口 | 先做基础标注对象和 UI | 添加标注后工程树可见 |
| [ ] | P1 | Layers | 图层控制面板 | 控制地图、区域、轨迹、标注显隐 | 开关各图层显示 |
| [ ] | P2 | View Finder | 视图定位器 | 先做回到当前区域/当前十字线 | 点击后视图定位 |

## 第三优先级：专业可用性增强

| 状态 | 优先级 | 工作项 | 目标行为 | 实现策略 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| [ ] | P1 | 工程浏览器右键菜单 | 文件、区域、标注组、界面右键管理 | 增加重命名、删除、隐藏、复制、属性等动作 | 右键各节点检查 |
| [ ] | P1 | 工程浏览器显隐逻辑 | 灯泡/显隐状态与视图联动 | 接入图层和对象可见性 | 切换后地图/切片同步变化 |
| [ ] | P1 | Region Processing Settings | 区域处理设置面板对齐 | 先整理 UI 参数和配置保存 | 打开、保存、恢复配置 |
| [ ] | P1 | Process Selected Regions | 处理选中区域入口 | 复用现有处理管线，不改算法 | 小数据轻量跑通 |
| [ ] | P1 | Display Processing Settings | 显示处理设置对齐 | UI 接现有显示参数 | 调整后视图刷新 |
| [hold] | P2 | Interface Tracing 算法 | 自动界面追踪 | 需要独立算法设计和样本验证 | 后续单独计划 |
| [ ] | P1 | 标注成果管理 | Annotation Group、目标对象管理 | 建立对象模型和工程保存 | 新建/保存/重开检查 |
| [ ] | P2 | 导出 GeoJSON | 导出 GIS 矢量成果 | 基于区域/标注对象输出 | 用 GIS 工具打开 |
| [ ] | P2 | 导出 DXF/DWG | 导出 CAD 成果 | 优先 DXF，DWG 后置 | CAD 打开检查 |
| [ ] | P2 | 导出 KMZ | 导出 Google Earth 成果 | 区域、轨迹、标注打包 | Google Earth 打开 |
| [hold] | P3 | 导出 SEG-Y | 专业雷达数据导出 | 需要确认数据结构和标准字段 | 后续单独验证 |
| [ ] | P2 | 导出 ASCII/CSV | 导出表格/文本成果 | 先做基础采样数据和标注导出 | 打开文件检查字段 |

## 第四优先级：高难度、后置

| 状态 | 优先级 | 工作项 | 目标行为 | 实现策略 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| [hold] | P3 | Geo Positioning | 地理定位/坐标配准 | 先保持只读和入口，避免误改数据 | 后续专项验证 |
| [hold] | P3 | 轨迹编辑与拼接 | 编辑导航轨迹、拼接区域 | 需要撤销、备份、坐标验证 | 后续专项验证 |
| [hold] | P3 | 大项目性能优化 | 大数据快速浏览和处理 | 缓存、抽稀、多线程 | 后续性能测试 |
| [ ] | P3 | 报告系统 | 输出报告和截图成果 | 等标注/导出稳定后实现 | 生成报告检查 |

## 建议下一轮开发顺序

1. 全局中文化清理，包括联动切片、测量框、右键菜单。
2. Scan 图右键菜单完整实现，优先完成复制图像和另存图像。
3. 测量功能收尾，确保总览和探索状态完全一致。
4. 探索页 Crosshair、Headers、Interface Tracing 参数窗优先接好。
5. 总览页 Select、Pan、Zoom、Layers、View Finder 完善。
6. 工程浏览器右键菜单、显隐逻辑和对象管理对齐。

## 开发约束

- 不主动修改已验证正确的算法流程。
- 不主动重构现有绘图显示核心逻辑。
- 每轮测试只做 10 分钟以内的轻量验证。
- 超过 10 分钟的复杂验证记录为手动验证项。
- 复杂功能可以先保留按钮、接口和占位弹窗，再逐步接真实实现。
- 每次阶段性完成后建议提交并推送远程，便于回滚和追溯。
