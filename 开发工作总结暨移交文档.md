# GPR Lab Pro 开发工作总结暨工作移交文档

更新时间：2026-05-19  
当前仓库：`GPR_Lab_Pro_V3`  
当前主分支：`main`  
当前最新提交：`321bc26 20260519-反馈问题第一次修改`

本文档用于后续开发人员快速接手当前软件工程。建议接手同事先阅读本文，再结合 `README.md`、`ARCHITECTURE.md` 和最近几次提交，让 Codex 在仓库根目录内辅助继续开发。

## 1. 仓库与访问

远程仓库地址：

```text
https://github.com/Anonymii/GPR_Lab_Pro_V3.git
```

当前本机工作目录：

```text
E:\code_management\GPR_V12_Pyside\GPR_Lab_Pro_V3
```

克隆方式：

```powershell
git clone https://github.com/Anonymii/GPR_Lab_Pro_V3.git
cd GPR_Lab_Pro_V3
```

如果 Windows 上 Git 提示 `dubious ownership`，可在确认路径可信后执行：

```powershell
git config --global --add safe.directory E:/code_management/GPR_V12_Pyside/GPR_Lab_Pro_V3
```

当前仓库可能出现未跟踪目录 `cache/`，这是地图瓦片或运行缓存，不属于源码提交范围，后续一般不要提交。

## 2. 环境配置

推荐环境：

- Windows 10/11
- Python 3.9 或更高版本
- Git
- 网络可访问 PyPI，首次安装依赖需要联网

项目依赖见 `requirements.txt`：

```text
numpy>=1.23,<2
scipy>=1.10,<1.14
PyWavelets>=1.4,<1.7
matplotlib>=3.7,<3.10
PySide6>=6.7,<6.11
pytest>=7.4,<9
```

首次安装依赖：

```powershell
.\install_deps.bat
```

该脚本会在仓库根目录创建 `.venv` 并安装 `requirements.txt`。

手动安装方式：

```powershell
python -m venv .venv
.\.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

运行软件：

```powershell
.\run_app.ps1
```

或：

```powershell
.\.venv\Scripts\python.exe -m gpr_lab_pro.app
```

如果直接双击运行，可使用：

```text
run_app.bat
```

## 3. 打包交付

打包脚本：

```powershell
.\build_release.ps1
```

脚本依赖 `.venv`，如果没有安装 `pyinstaller` 会自动安装。打包完成后生成：

```text
release\GPR_Lab_Pro_V4
release\GPR_Lab_Pro_V4.zip
```

交付给客户时优先发送整个 `GPR_Lab_Pro_V4.zip`，客户解压后运行：

```text
GPR_Lab_Pro_V4.exe
```

或：

```text
release_launcher.bat
```

如果启动失败，软件会尝试生成：

```text
startup_error.log
logs\startup_trace.log
```

这些日志可用于排查 Qt 插件、依赖、路径或运行时异常。

## 4. 总体架构

项目是 PySide6 桌面软件，采用“UI 层、应用控制层、领域模型、处理流水线、算法引擎、渲染适配、基础设施、IO”分层组织。

核心目录：

```text
gpr_lab_pro/
  app/                  应用入口、上下文、控制器
  app/controllers/      Project/Dataset/Pipeline/Display/Export/Task 控制器
  domain/               领域枚举和状态模型
  infrastructure/       日志、设置、在线地图配置、后台 worker
  io/                   DAT/3D Radar 导入、项目保存读取
  processing/           处理模块注册、流水线、运行时、算法桥接
  processing/modules/   频域和时域处理模块实现
  processing/transforms 时频域转换桥接，含 IFFT/CZT/ISDFT
  render/adapters/      B-scan、C-scan、A-scan 等显示数据构造
  resources/            总览地图 Web 资源等
  ui/                   PySide6 主窗口和视图控件
  plugins/              插件接口和示例
```

主入口：

```text
gpr_lab_pro/app/main.py
gpr_lab_pro/application.py
gpr_lab_pro/ui/main_window_v12.py
```

应用启动链路：

```text
python -m gpr_lab_pro.app
-> gpr_lab_pro/app/__main__.py
-> gpr_lab_pro/app/main.py
-> GPRApplication
-> launch_main_window(GPRApplication())
```

## 5. 关键控制器职责

`gpr_lab_pro/application.py`

- 创建 `ApplicationContext`
- 组装所有控制器
- 负责导入数据、打开工程、恢复工程状态、初始化处理流程

`gpr_lab_pro/app/context.py`

- 保存全局状态对象
- 持有信号、设置、线程池、导入服务、项目存储、处理引擎、流水线执行器

`ProjectController`

- 工程创建、打开、保存
- 文件、区域、地图影像、界面追踪、标注等项目状态管理
- 从项目区域构建当前运行用 `DatasetRecord`

`DatasetController`

- 调用导入服务读取 `.dat` 或 `.3dra`
- 将导入结果封装为 `DatasetRecord`

`PipelineController`

- 管理处理步骤
- 初始化、添加、删除、运行流水线
- 把结果写入 `ResultState`

`DisplayController`

- 根据当前数据集、处理结果和选择状态生成 `DisplayData`
- 构建 B-scan、T-scan、C-scan、Trace line 等显示数据
- 管理 B-scan 横坐标米制距离轴、双轴显示、选点联动

`ExportController`

- 流程模板保存/加载
- 处理结果导出

`TaskController`

- 后台任务调度
- 导入和处理时避免 UI 阻塞

## 6. 数据导入

支持数据源：

- 普通 `.dat`
- 3D Radar `.3dra`
- 已解包的 `.3dra.extracted` 文件夹

入口文件：

```text
gpr_lab_pro/io/importers/dat_importer.py
gpr_lab_pro/io/importer.py
gpr_lab_pro/io/dat_loader.py
gpr_lab_pro/io/importers/three_d_radar_importer.py
```

导入分发逻辑：

- `DatImporterService.import_file()` 根据文件后缀选择导入器
- `.3dra` 或 `.3dra.extracted` 进入 `ThreeDRadarImporter`
- 其他进入 `GPRDataImporter`

3D Radar 导入要点：

- 默认读取 `data0.cube`
- 读取 `metadata.xml`
- 读取 `triggers.trg`
- 读取 `positions_internal.tsv`
- 如果传入 `.3dra` 且相邻存在 `.3dra.extracted` 文件夹，优先使用已解包目录
- 如果没有已解包目录，会尝试寻找 `Archiver.exe` 执行解包

当前距离轴策略：

- B-scan 每一道的距离优先使用 3D Radar `metadata.xml` 中 `distance_trigger/distance`
- 如果没有稳定距离触发信息，再回退到 GPS/导航采样累积距离
- 如果 GPS 也不可用，使用默认间距 `0.05 m`
- 这部分逻辑在 `DisplayController._trace_distance_axis_m()` 和相关 helper 中

这样做的原因是 GPS 可能缺失或跳变，而距离触发步长更适合为每一道稳定赋予横向距离。

## 7. 处理流程与算法模块

处理流程核心文件：

```text
gpr_lab_pro/processing/pipeline.py
gpr_lab_pro/processing/pipeline/executor.py
gpr_lab_pro/processing/pipeline/cache_manager.py
gpr_lab_pro/processing/runtime.py
gpr_lab_pro/processing/catalog_v11.py
gpr_lab_pro/processing/module_registry_v11.py
```

处理模块分组：

- 时频域桥接：`ifft`、`czt`、`isdft`
- 频域预处理：相位校正、频域背景去除、Hann 窗、小波等
- 频域干扰抑制：主动/被动干扰抑制
- 时域预处理：dewow、t0、t0_fb
- 去背景：均值、中值、滑动、SVD、RPCA、Top mute、高通、自适应
- 增益补偿：线性、指数、球面、SEC、AGC、TGC+AGC、背景估计、自适应对比度
- 一维滤波、二维滤波、高级滤波
- 迁移成像：Kirchhoff、Stolt
- 小波与重构：SWT、DWT、压缩感知重构

后续开发时，如需新增处理算法，推荐路径：

1. 在 `processing/module_registry_v11.py` 中确认模块归属。
2. 在 `processing/catalog_full.py` 或相关 catalog 中增加操作定义和参数。
3. 在 `processing/modules/` 或 `processing/transforms/` 中实现算法。
4. 通过 `PipelineController` 跑通流程并检查 `ResultSnapshot`。
5. 只对新增算法做局部验证，避免影响已有处理链。

## 8. UI 与主要功能

主界面文件：

```text
gpr_lab_pro/ui/main_window_v12.py
```

该文件较大，集中承担主窗口、功能区、总览视图、探索视图、测量、界面追踪、显示渲染等 UI 逻辑。后续开发时建议让 Codex 先搜索目标函数，不要整文件重构。

主要界面：

- 功能区 Ribbon：主页、视图、数据
- 总览页 Overview：工程区域总览、地图/轨迹、深度切片预览、测量
- 探索页 Explore：B-scan、T-scan、C-scan、Trace line 联动显示
- 工程树：文件、区域、地图影像、界面线等对象管理
- 处理面板：流程步骤、参数、运行处理
- 显示设置：B-scan/C-scan 属性、色图、反色、对比度、线宽、双轴显示等

已实现或已接入的用户功能：

- 新建/打开/保存工程
- 导入 `.dat`、`.3dra`、`.3dra.extracted`
- 区域管理
- 频域、时频域、时域处理链
- B-scan、C-scan、T-scan、Trace line 联动浏览
- B-scan 横坐标米制显示
- 深度/时间双坐标轴显示
- 视图同步缩放和平移
- 测量长度、面积
- 地面时间标记和预览增益
- 界面追踪，新建/重命名/复制/删除/显示隐藏/拾取/补线/平滑
- 双曲线预览叠加
- 离线地图和在线高德地图接入
- 地图影像、项目树和总览预览
- 视图截图复制和保存
- PyInstaller 打包

需要继续关注的功能：

- 双曲线功能目前以显示预览为主，后续需要根据用户实际测试继续完善交互、速度/介电常数参数、拟合逻辑。
- 3D Radar 对标还在持续反馈中，界面布局和显示细节仍可能继续调整。
- 地图和标注功能已验证过的逻辑不要轻易重构。
- 数据处理主链路已基本验证，不要为了 UI 问题改动底层矩阵处理流程。

## 9. 最近一次修改摘要

最近提交：

```text
321bc26 20260519-反馈问题第一次修改
```

本次提交主要解决：

- 视图工具栏颜色区域移除“雷达数据颜色”文本，只保留“反色”勾选框。
- B-scan 道距离优先使用 3D Radar `distance_trigger` 稳定步长。
- B-scan 双击选取十字线时，距离到道号映射改为连续轴插值，减少 GPS 缺失导致的跳段。
- B-scan 和 C-scan 使用同一网格列宽，对齐上下 scan 的物理横向宽度。
- 保持显示层修改，不触碰核心雷达数据矩阵处理。

最近几次相关提交：

```text
321bc26 20260519-反馈问题第一次修改
f2c1621 修正显示测量与渲染交互
68af5b3 调整3D Radar显示细节
8a68203 完善3D Radar对标显示功能
02adb17 Add Time Ground display preview
```

## 10. 地图与在线配置

地图配置文件：

```text
online_map.local.json
```

该文件在运行根目录生成，通常包含高德地图 key 和安全密钥，不建议提交到仓库。

相关代码：

```text
gpr_lab_pro/infrastructure/online_map.py
gpr_lab_pro/resources/overview/amap_overview.html
gpr_lab_pro/ui/overview_quick_map.py
gpr_lab_pro/ui/main_window_v12.py
```

离线瓦片目录名：

```text
offline_tiles
```

程序会在运行根目录或其父目录查找 `offline_tiles`。打包时 `build_release.ps1` 会尝试从仓库父目录复制 `offline_tiles` 到发布包。

## 11. 项目文件与运行数据

工程保存格式：

```text
*.gpr.json
```

项目存储代码：

```text
gpr_lab_pro/io/project_store.py
```

处理结果会持久化到工程目录下的结果文件中，工程树、区域、界面、显示状态、处理流程等通过 `ProjectStore` 保存。

运行缓存：

- `cache/`：地图瓦片和运行缓存，不提交。
- `__pycache__/`：Python 字节码缓存，不提交。
- `release/`：打包输出，通常不作为源码提交。
- `.venv/`：本地虚拟环境，不提交。

## 12. 开发规范与注意事项

优先遵守以下原则：

- 修改前先运行 `git status --short --branch`。
- 不要把 `cache/`、`.venv/`、`release/`、`__pycache__/` 提交进仓库。
- 当前已验证的数据处理流程和地图显示标注功能不要轻易改动。
- UI 交互问题优先在 UI 层或 `DisplayController` 解决，不要把拖动滑块、十字线线宽、视图布局等操作关联到底层数据重算。
- `main_window_v12.py` 很大，改动时通过 `rg` 定位函数，做小范围补丁。
- 一些旧文件存在中文乱码显示，不要为了“美化”整文件转码，避免引入大面积无关 diff。
- 每次完成后做轻量验证即可：语法编译、关键 import、局部函数验证。

推荐轻量验证命令：

```powershell
python -B -c "import tokenize; paths=[r'gpr_lab_pro\ui\main_window_v12.py',r'gpr_lab_pro\app\controllers\display_controller.py']; [compile(tokenize.open(p).read(), p, 'exec') for p in paths]; print('syntax ok')"
python -B -c "from gpr_lab_pro.app.controllers.display_controller import DisplayController; from gpr_lab_pro.ui.main_window_v12 import MainWindow; print('imports ok')"
git diff --check
```

说明：Windows 上如果 `py_compile` 因 `__pycache__` 权限失败，可使用上面的 `compile(tokenize.open(...).read())` 方式，它不会写 `.pyc`。

## 13. 与 Codex 协作建议

接手同事可以在仓库根目录启动 Codex，并先给 Codex 这样的上下文：

```text
请先阅读 开发工作总结暨移交文档.md、README.md、gpr_lab_pro/application.py、
gpr_lab_pro/app/controllers/display_controller.py、gpr_lab_pro/ui/main_window_v12.py。
后续开发重点是对标 3D Radar 完善功能和布局。不要破坏已经验证的数据处理流程和地图标注显示。
修改前先检查 git status，修改后只做轻量验证。
```

常用 Codex 工作方式：

- 先让 Codex 搜索相关函数，不要直接全局重构。
- 让 Codex 给出涉及文件和风险点，再开始改。
- UI 卡顿问题优先检查是否错误触发 `publish_display()`、图像重建、处理流水线重跑。
- 坐标单位问题优先检查 `DisplayController` 的 `DisplayData.meta` 和 UI 层坐标换算函数。
- 导入数据问题优先检查 `io/importer.py`、`dat_loader.py`、`three_d_radar_importer.py`。
- 处理算法问题优先检查 `processing/modules/`、`processing/transforms/`、`processing/pipeline/`。

建议每个用户反馈独立提交：

```powershell
git status --short
git add <本次修改文件>
git commit -m "YYYYMMDD-反馈问题第N次修改"
git push origin main
```

如果 Git 未配置用户名：

```powershell
git config user.name "Anoymii"
git config user.email "chenyulinupc@163.com"
```

## 14. 后续优先级建议

第一优先级：

- 根据用户手动测试反馈，继续修复 3D Radar 对标差异。
- 重点关注 B-scan/C-scan/T-scan/Trace line 的布局、坐标单位、联动和响应速度。
- 保持 UI 操作流畅，避免滑块拖动和十字线线宽变化触发数据重算。

第二优先级：

- 完善双曲线功能，从预览叠加扩展到用户期望的交互和参数控制。
- 完善 3D Radar 数据源的距离、宽度、通道位置等元数据利用。
- 补齐导出界面点云等已预留功能。

第三优先级：

- 拆分 `main_window_v12.py`，把视图控件、总览地图、界面追踪、测量逻辑逐步模块化。
- 清理旧文档或旧代码中的编码乱码。
- 增加关键逻辑的自动化测试，特别是坐标换算、导入元数据解析、项目保存恢复。

## 15. 接手检查清单

接手后建议按顺序确认：

1. 能成功克隆仓库。
2. 能执行 `.\install_deps.bat`。
3. 能执行 `.\run_app.ps1` 启动软件。
4. 能导入一份 `.dat` 或 `.3dra.extracted` 数据。
5. 能运行一次已有处理流程并显示 B-scan/C-scan/T-scan/Trace line。
6. 能保存并重新打开工程。
7. 能修改一个小 UI 文本并通过轻量验证。
8. 能提交并推送到 `origin/main`。

完成以上步骤后，后续功能开发基本可以借助 Codex 无障碍推进。
